// Checkout Page - No Header/Footer Layout
export default function CheckoutLayout({ children }) {
  return (
    <div style={{ minHeight: '100vh', background: '#fafafa' }}>
      {children}
    </div>
  );
}
