/* ==========================================
   LOGIN LAYOUT - Simplified
   
   CSS is loaded in page.js for consistent client-side navigation
   ========================================== */

export const metadata = {
  title: 'Login - Startup India Incubation',
  description: 'Sign in to access the Startup India Incubation Platform',
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
