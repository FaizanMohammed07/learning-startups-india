/* ==========================================
   SIGNUP LAYOUT - Simplified
   
   CSS is loaded in page.js for consistent client-side navigation
   ========================================== */

export const metadata = {
  title: 'Sign Up - Startup India Incubation',
  description: 'Create your account and join the startup ecosystem',
};

export default function SignupLayout({ children }) {
  return <>{children}</>;
}
